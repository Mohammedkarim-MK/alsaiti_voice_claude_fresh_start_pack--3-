{
  "name": "alsaiti-voice-worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Long-running LiveKit voice agent for Alsaiti Voice. Answers real calls, qualifies, creates exactly one lead per call.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test test/logic.test.js test/call.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4"
  },
  "optionalDependencies": {
    "@livekit/agents": "^1.6.0",
    "@livekit/agents-plugin-openai": "^1.6.0",
    "livekit-server-sdk": "^2.17.0"
  }
}